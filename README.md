# ClubQ API deployment

This repository contains the compiled Erlang shipment for the ClubQ API.
Render uses it to build the public deployment image. The application source stays in the private ClubQ repository.
